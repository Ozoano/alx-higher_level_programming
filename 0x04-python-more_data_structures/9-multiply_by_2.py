#!/usr/bin/python3
def multiply_by_2(a_dict):
    mult = {}
    copy_dict = a_dict[:]

    if copy_dict is None:
        return None
    for k, v in copy_dict.items():
        for item in value:
            mult.append(v * 2)
        return copy_dict
